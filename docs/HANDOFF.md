# حالة المشروع — ملف تسليم بين الجلسات

> آخر تحديث: 2026-08-21 مساءً (ليلة انطلاق الدوري). اقرأ `CLAUDE.md` أولًا ثم هذا الملف.
> لبدء جلسة جديدة: "اقرأ CLAUDE.md و docs/HANDOFF.md ثم كمّل قائمة المتبقي".

## ما هو مكتمل ويعمل (مُختبر)

- **20 صفحة** تعمل ببيانات الدوري الحقيقي محليًا (بلا سيرفر خارجي): الرئيسية، المباريات
  (تجميع بالفترة + مباريات متوازية على ملعبين)، الترتيب + شجرة الكأس، صفحة المباراة
  (أحداث/تشكيلات/إحصائيات/مواجهات + مشاركة واتساب)، كونسول التسجيل الكامل (ضغطتان،
  تراجع 5ث، إنذار ثانٍ = طرد، كروت قوة، تشكيلة أساسية، اعتماد PIN=1234)، الفرق واللاعبون،
  الإحصائيات (+ شباك نظيفة + موقوفون)، التوقعات، المجتمع (نشر محلي)، الإشعارات، البوستر
  الحي، وضع TV، اللائحة، مهامي، أنا، لوحة الأدمن، **محرر الجدول** `/admin/schedule`
  (تغيير يوم/فترة/ملعب أي مباراة + فحص تعارضات حي + اقتراح أقرب فترة).
- محركات نقية مُختبرة (22 اختبار): `lib/standings/compute.ts`،
  `lib/scheduling/conflicts.ts`، `lib/discipline/suspensions.ts`.
- قاعدة صارمة مطبقة: كل مباراة تعرض دائمًا (اليوم + الفترة + الملعب).

## مكتمل: إصلاح الأخطاء الـ 21 التي أكدتها المراجعة العدائية ✅

كل البنود منفذة والفحوص خضراء: `typecheck` ✓ · `test` 22/22 ✓ · `build` 20 صفحة ✓.
ملاحظات تنفيذ مهمة:

- بند 2 اكتمل فعليًا الآن: المخزن يمرر `structuralSideTokens(matches)`
  للفحص وللاقتراح (كان المحرك جاهزًا والمخزن لا يستخدمه).
- توسيع W_/L_ صار موسومًا بالمصدر والقطب (`SideToken {id, source, pick}`):
  فائز النصف × خاسره لا يتقاطعان أبدًا — بدونها كان الجدول الحقيقي
  (ثالث 00:00 + نهائي 00:30) يُعلَّم بتعارض فجوة زائف.
- أُضيف `vitest.config.ts` (alias `@/`) — اختبارات conflicts كانت تفشل في
  التحميل لأن المحرك يستورد `slotToMinutes` من seed.

قائمة البنود المنفذة:

1. [x] نتائج الإقصائيات تُشتق 0-0 → scoreOf/knockoutWinner في store يحلّان الأطراف قبل deriveScore.
2. [x] conflicts.ts: مقارنة رموز خام تفوّت تعارض النهائي/الثالث مع نصفَي النهائي
       → structuralSideTokens + فجوات بالدقائق + تمريره من المخزن.
3. [x] suggestNearestSlot يتخطى (يوم+فترة+ملعب) الحالي فقط.
4. [x] فترة النهائي 00:30 → seed.allSlots والترتيب بالدقائق.
5. [x] التراجع/الحذف يمسح الطرد التلقائي المرافق (linkedTo يُختم عند التوليد).
6. [x] التراجع/الحذف عن تبديل يعيد بناء lineupOverrides من أحداث التبديل المتبقية.
7. [x] التراجع عن هدف مضاعف يعيد تفعيل الكارت (event.powerCard يُختم عند الاستهلاك؛
       لو خانة الكارت النشط مشغولة يُرد الكارت لرصيد الفريق بدلًا من الدهس).
8. [x] clearPowerCard يرد الكارت لرصيد الفريق ويُسجَّل في التدقيق.
9. [x] البدلاء: زر البديل يمنع الموقوف (disabled + 🚫) وحارس في onBenchTapForSub.
10. [x] PrepSheet يستبعد الموقوفين من الاختيار المبدئي (المحفوظ والافتراضي).
11. [x] حارس period="ended" في toggleClock وadvancePeriod.
12. [x] الرئيسية تعرض كل المباريات الجارية (المتوازية) لا أول واحدة فقط.
13. [x] محرر الجدول: خيارات الفترة من seed.allSlots (تظهر 00:30 للنهائي).
14. [x] صفحة المباريات: الليلة الافتراضية lazy (nightOverride null + حساب كل render).
15. [x] الشجرة: العنوان والبطاقات من matchDay (formatNight) لا نصًّا يدويًا.
16. [x] الرئيسية: كروت "انتهت الليلة" تعرض الفترة + شارة الملعب.
17. [x] stats: الموقوفون يُصفّون بعد قضاء الإيقاف (فلتر forMatchId غير معتمدة).
18. [x] stats: مفاتيح React بـ player id بدل الاسم.
19. [x] صفحة الفريق: المباريات بالأكواد المحلولة (تشمل الإقصائيات بعد تحدد الأطراف).
20. [x] TV: المكونات الخمسة على مستوى الملف (لا remount كل 5 ثوانٍ).
21. [x] tests/conflicts.test.ts: واجهة sideTokens + 4 حالات نهائي×نصف
       (الجدول الحقيقي بلا تعارض، نفس الفترة، فجوة 30د، حد الليلة).

## قرارات مهمة حديثة

- إتاحة ملعب 2 في الـ seed صارت تشمل ليلة 11/9 فترة 23:00 (نصفا النهائي متوازيان).
- الفجوة الدنيا بين مباراتَي فريق = 40 دقيقة (فترة واحدة). حد الليلة = مباراتان.
- التوقعات: نتيجة مضبوطة 3 نقاط، اتجاه صحيح 1.
- التقاطع البنيوي للرموز يستبعد (فائز × خاسر) نفس النصف — قاعدة نطاق موثقة
  في `lib/scheduling/conflicts.ts`.

## مكتمل: قاعدة Supabase (schema + seed + أنواع) والرفع إلى GitHub ✅

- **GitHub**: الريبو https://github.com/IbrahimBadawy/halaqat-league (private، فرع main).
- **Supabase** (المشروع `mgepypcbactyxiqokloi`): 3 migrations مطبقة على السحابة
  ومحفوظة في `supabase/migrations/`:
  1. `initial_schema_phase0` — 21 جدولًا من §7 (حسابات/عضوية، ملاعب+إتاحة،
     فرق+لاعبون، مراحل/مجموعات/مباريات، أحداث/تشكيلات/تقارير، تعديلات نقاط،
     كروت قوة، توقعات، audit_log) + RLS على كل جدول + `has_league_role()`.
  2. `seed_first_league` — الدوري الحقيقي كاملًا (تحقق بالعد: 10 فرق، 70 لاعبًا،
     24 مباراة m1..m24، ملعبان + 3 فترات إتاحة، 4 كروت × 10 فرق).
  3. `restrict_has_league_role_execute` — إغلاق تحذير advisor (سحب execute من anon).
- الأنواع المولدة: `lib/supabase/types.ts`. القيم في `.env.local` (غير مرفوع).
- مبادئ المخطط: النتيجة تُشتق من الأحداث (match_events.value + linked_to +
  power_card)، الأطراف placeholders في home_side/away_side مع home_team_id
  يُملأ عند الحسم، matches.code = m1..m24 وplayers.code = A1-4 لتطابق الواجهة.

## مكتمل: الموقع يعمل على Supabase + منشور على GitHub Pages ✅

**العمارة الحالية** (الصفحات الـ20 لم تتغير — نفس واجهة `useLeague()`):

- **القراءة**: `lib/league/remote.ts` يجلب كل الجداول بمفتاح publishable (RLS
  قراءة عامة) ويحوّلها لنفس أشكال `LeagueSeed`/`PersistedState` بمفاتيح
  الأكواد (m1..، A1..، A1-4). الـ seed المدمج (JSON) يبقى fallback فوريًا
  عند انقطاع الشبكة، وأكواد generateStaticParams تُعدَّد منه.
- **الكتابة**: حصريًا عبر Edge Function `live-write` (service-role داخليًا،
  تحقق PIN، أفعال محددة بأعمدة whitelisted — لا كتابة أنون مباشرة على
  الجداول). PIN الافتراضي 1234 (secret `LIVE_PIN` في الدالة؛ العميل يقرؤه
  من localStorage مفتاح `halaqat-live-pin`). التحديث المتفائل محليًا +
  إرسال غير متزامن.
- **Realtime**: قناة واحدة على 7 جداول حية → إعادة جلب مدمجة (350ms debounce).
  ساعة المباراة في `matches.clock` (jsonb) فتتزامن بين الأجهزة.
- **المشتقات**: `lineupOverrides` صارت تُشتق من أحداث التبديل (التراجع يصحح
  التشكيلة تلقائيًا)، وكروت القوة من `card_usages`
  (approved=مفعّل، applied=مستهلك، cancelled=مسترد).
- **طابور كتابة مستمر** (`lib/league/queue.ts`): كل كتابة تمر بطابور في
  localStorage وتُرسل بالترتيب، وتُعاد المحاولة عند حدث `online` أو كل 15 ثانية.
  العدّادان `store.pendingWrites` و`store.droppedWrites` ظاهران في شارة
  الكونسول وصفحة «أنا». تفاصيل حرجة أثبتتها المراجعة:
  - `liveWrite` يميّز **offline** (لم يصل الطلب → إعادة بلا حد، حفاظًا على
    ترتيب الأحداث) عن **reject** (وصل ورُفض → 3 محاولات ثم يُسقط العنصر).
    بدون هذا التمييز كان عنصر فاسد واحد يسدّ الطابور فلا يصل أي هدف بعده.
  - كل الإدراجات `upsert ... ignoreDuplicates` بمعرّف من العميل (الأحداث،
    المنشورات، التدقيق، تعديلات النقاط، التشكيلات): إعادة إرسال طلب نجح ولم
    تصل استجابته لا تكرر صفًا ولا تفشل.
  - إعادة الجلب اللحظية تتأجل ما دام `pendingWrites > 0` حتى لا تمحو لقطةُ
    القاعدة حدثًا سُجّل للتو وما زال في الطابور.
- **المجتمع والتوقعات في القاعدة**: `posts` (قراءة عامة + `bump_post_likes`
  للإعجاب الذري) و`predictions` بمفتاح جهاز (`halaqat-device-key`) —
  التوقعات تنجو من مسح المتصفح وتتبع صاحبها لحين ربطها بحساب.
  أفعال الجمهور (نشر/إعجاب/توقع) لا تتطلب PIN؛ ما يمس نتيجة أو ترتيبًا يتطلبه.
- **محلي على الجهاز فقط**: الدور المختار (localStorage
  `halaqat-league-local-v1`). «تعبئة بيانات تجريبية» أُزيلت من `/me` لأن
  القاعدة مشتركة، و«إعادة الضبط» تمس هذا الجهاز فقط.
- **النشر**: تصدير ثابت (`output: "export"`، 78 صفحة، basePath
  `/halaqat-league`) عبر `.github/workflows/deploy.yml` إلى
  **https://ibrahimbadawy.github.io/halaqat-league/** — الريبو صار public
  (شرط GitHub Pages المجاني).

### المتبقي للمراحل التالية

- Auth حقيقي (username → بريد داخلي) + league_members ثم إزالة بوابة PIN
  وتشديد سياسات الكتابة على الأدوار، وتغيير `LIVE_PIN` من لوحة Supabase.
- الإشراف على المجتمع (حذف/حظر/كلمات ممنوعة) — المنشورات الآن عامة بلا فلترة.
- Push notifications، وصور المشاركة المولدة (@vercel/og)، ومولد الجدولة T1.
- تحذير advisor الوحيد المتبقي مقصود: `has_league_role` لازم تبقى قابلة
  للتنفيذ من `authenticated` لأن سياسات RLS تستدعيها (تُرجع صلاحية المستدعي
  نفسه فقط).

## المؤجل للمراحل التالية (بالترتيب المقترح)

مولد الجدولة T1 (سيناريوهات) · معالج إنشاء الدوري · Push · صور المشاركة المولدة
(@vercel/og) — البرومبتات الجاهزة في `docs/prompt-claude-code.md`.
